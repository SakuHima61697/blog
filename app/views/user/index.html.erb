<div class="user-index">
    <h1 class="page-title">ユーザー一覧</h1>
    <% @users.each do |user| %>
            <% if user.image.url %>
                <%= image_tag user.image.url, class: "profile-image" %>
            <% end %>
            <div class="user-name-index">
                <%= link_to(user.name, "/blogs/user/#{user.id}") %>
                <br></br>
            </div>
    <% end %>
    <br></br>
    <%= paginate @users %>
</div>